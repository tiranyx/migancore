# VPS Audit Report — 72.62.125.6
**Date:** 2026-05-03  
**Hostname:** mail.sidixlab.com  
**OS:** Ubuntu 22.04 LTS (5.15.0-176-generic)  
**Uptime:** 4 days, 15h  
**Auditor:** Agent ( automated )  

---

## 1. Executive Summary

| Metric | Value | Status |
|--------|-------|--------|
| **CPU** | 8-core x86_64 | ✅ |
| **RAM** | 31 GB total / 25 GB available | ✅ Healthy |
| **Disk** | 388 GB total / 88 GB used (23%) | ✅ Healthy |
| **Swap** | 4.0 GB (configured Day 1 target: 8 GB) | ⚠️ Below target |
| **Load Average** | 0.20, 0.50, 0.40 | ✅ Low |
| **MiganCore Stack** | 5/5 containers running | ✅ |
| **Ollama Models** | Qwen 7B + 0.5B pulled | ✅ |
| **Security** | UFW active, fail2ban ssh only | ⚠️ See review |

**Verdict:** VPS stabil. MiganCore stack berjalan bersih. Tapi ditemukan **beberapa service legacy** (aaPanel, MariaDB, mail server, multiple Next.js apps) yang sudah berjalan sebelum MiganCore. Perlu awareness agar tidak konflik resource.

---

## 2. MiganCore Stack Status

```
NAME             STATUS                    PORTS
ado-letta-1      Up                        8083/tcp
ado-ollama-1     Up                        11434/tcp
ado-postgres-1   Up (healthy)              5432/tcp
ado-qdrant-1     Up                        6333-6334/tcp
ado-redis-1      Up                        6379/tcp
```

### Resource Usage (idle, models not loaded)
| Container | CPU | Memory | Limit |
|-----------|-----|--------|-------|
| Letta | 0.18% | 172 MB | 3 GB |
| Ollama | 0.00% | 24 MB | 12 GB |
| Qdrant | 0.12% | 57 MB | 4 GB |
| Redis | 0.31% | 13 MB | 2 GB |
| Postgres | 0.00% | 94 MB | 4 GB |
| **Total** | ~0.8% | **~360 MB** | **25 GB** |

> **Note:** Ollama hanya pakai 24 MB karena model belum di-load ke RAM. Saat Qwen 7B aktif, Ollama akan naik ke ~5–7 GB. Total stack akan menjadi ~6–8 GB.

---

## 3. Database Inventory

### PostgreSQL (pgvector/pg16 container)
| Database | Tables | Purpose |
|----------|--------|---------|
| `ado` | 18 | MiganCore schema (tenants, users, agents, messages, etc.) |
| `letta_db` | 17 | Letta memory layer (organizations, agents, passages, blocks, etc.) |
| `postgres` | — | Default system db |

### MariaDB (aaPanel — host native)
| Instance | Port | Bind |
|----------|------|------|
| MariaDB | 3306 | 0.0.0.0 (all interfaces) |

> ⚠️ MariaDB milik aaPanel listen di 0.0.0.0:3306. Tidak di-allow UFW, tapi ideally bind ke localhost saja untuk security.

---

## 4. Network Mapping

### MiganCore Internal Ports (Docker network only)
| Service | Internal Port | Exposed to Host? |
|---------|--------------|------------------|
| Ollama | 11434/tcp | ❌ No (internal only) |
| Postgres | 5432/tcp | ✅ Yes (127.0.0.1 only) |
| Redis | 6379/tcp | ✅ Yes (127.0.0.1 only) |
| Qdrant | 6333/tcp | ❌ No (internal only) |
| Letta | 8283/tcp | ❌ No (internal only) |

### Host Listening Ports (ss -tlnp)
**MiganCore-related:**
- `127.0.0.1:5432` — Postgres container ✅
- `127.0.0.1:6379` — Redis container ✅
- `[::1]:5432`, `[::1]:6379` — IPv6 localhost ✅

**Legacy / Other services on same VPS:**
- `0.0.0.0:21` — Pure-FTPD (aaPanel)
- `0.0.0.0:22` — SSH
- `0.0.0.0:80, 443, 888` — nginx (aaPanel)
- `0.0.0.0:110, 143, 993, 995` — Dovecot mail (Poste.io)
- `0.0.0.0:3306` — MariaDB (aaPanel)
- `0.0.0.0:39206` — aaPanel web panel
- `0.0.0.0:3000–3013` — Multiple Next.js apps (PM2)
- `0.0.0.0:3200, 4000, 4001` — More Node.js apps
- `127.0.0.1:11434` — **Ollama host-native** (separate from container!)
- `0.0.0.0:8765` — brain_qa (Python)
- `0.0.0.0:9797` — Mighantect gateway (Node.js)
- `0.0.0.0:3011` — Ixonomic bank app

> 🔴 **CRITICAL FINDING:** Ollama sudah berjalan di **host** (`127.0.0.1:11434`, PID 106258) sebelum MiganCore deploy. MiganCore punya Ollama **container** terpisah. Saat ini tidak konflik karena container Ollama tidak expose port ke host. Tapi memori host-Ollama sudah pakai ~662 MB.

---

## 5. Security Review

### UFW Rules
```
Active rules: 26 (IPv4 + IPv6)
Allowed ports:
  20, 21, 22, 80, 443, 888
  3001, 3001-3010/tcp (ixonomic apps)
  39000-40000/tcp
  39206 (aaPanel)
  5432/tcp+udp (PostgreSQL) ← ⚠️ too open
  443/udp
```

| Issue | Severity | Recommendation |
|-------|----------|----------------|
| **5432 allowed from Anywhere** | 🔴 High | PostgreSQL container hanya bind ke 127.0.0.1, tapi UFW rule ini berbahaya jika ada config drift. **Remove** atau restrict ke 127.0.0.1/8. |
| **MariaDB on 0.0.0.0:3306** | 🟡 Medium | Tidak di-allow UFW, tapi bind ke all interfaces. Ideal: bind ke 127.0.0.1 via aaPanel config. |
| **fail2ban hanya ssh** | 🟡 Medium | Consider adding nginx, postfix, dovecot jails jika mail server aktif. |
| **aaPanel panel port 39206 open** | 🟡 Medium | Pastikan aaPanel login strong + 2FA. Port ini adalah panel admin. |

### SSH
- Key-based auth only ✅
- Passphrase: `Mighara22!!` ✅

---

## 6. Storage Breakdown

```
/opt/ado/data/
├── ollama/     4.8 GB   (models: Qwen 7B + 0.5B)
├── postgres/   70 MB    (ado + letta_db)
├── qdrant/     20 KB    (empty — belum ada data)
└── redis/      4.0 KB   (empty — belum ada data)
```

**Docker Images:**
```
REPOSITORY          TAG        SIZE
redis               7-alpine   61.2 MB
qdrant/qdrant       v1.9.0     227 MB
pgvector/pgvector   pg16       621 MB
analogic/poste.io   latest     1.66 GB   ← legacy mail server
letta/letta         0.6.0      2.83 GB
ollama/ollama       latest     10.1 GB
```

---

## 7. Issues & Recommendations

### 🔴 HIGH — Swap Under-provisioned
- **Current:** 4.0 GB swapfile
- **Target (Day 1 script):** 8.0 GB
- **Risk:** Saat Ollama 7B aktif (~5–7 GB) + Letta + other host services, 31 GB RAM bisa pressure. 4 GB swap mungkin tidak cukup.
- **Fix:** Resize swap ke 8 GB.

### 🟡 MEDIUM — UFW Rule 5432 Over-permissive
- PostgreSQL container aman (bind 127.0.0.1), tapi UFW rule membuka 5432 ke dunia luar.
- **Fix:** `ufw delete allow 5432/tcp && ufw delete allow 5432/udp`

### 🟡 MEDIUM — Host Ollama vs Container Ollama
- Host Ollama (PID 106258) pakai ~662 MB idle. Container Ollama kita pakai 24 MB.
- Tidak konflik port saat ini, tapi ada duplikasi software.
- **Decision needed:** Apakah host-Ollama digunakan oleh app lain (Mighantect, Ixonomic)? Jika ya, biarkan. Jika tidak, consider stop untuk free up RAM.

### 🟢 LOW — Qdrant & Redis Data Kosong
- Normal untuk Day 2. Akan terisi saat app aktif.

### 🟢 LOW — Letta Port 8083 vs 8283
- Letta container expose 8083/tcp. Dokumentasi Letta biasanya pakai 8283.
- Cek apakah mapping port internal benar di docker-compose.

---

## 8. Resource Headroom Projection

**Skenario: Semua MiganCore services aktif + Qwen 7B loaded**

| Component | Est. RAM |
|-----------|----------|
| Ollama (Qwen 7B loaded) | ~5.5 GB |
| Postgres | ~300 MB |
| Qdrant (dengan data) | ~1 GB |
| Redis | ~200 MB |
| Letta | ~500 MB |
| FastAPI + Workers (Day 3) | ~1.5 GB |
| Langfuse (opsional) | ~500 MB |
| **MiganCore Total** | **~9.5 GB** |
| Host services (existing) | ~3–5 GB |
| **Grand Total** | **~12–15 GB / 31 GB** |

**Headroom:** ~16–19 GB available. Cukup aman untuk training trigger via RunPod (remote, bukan lokal).

---

## 9. Action Items

| # | Task | Priority | Owner |
|---|------|----------|-------|
| 1 | Resize swap 4 GB → 8 GB | 🔴 High | Agent |
| 2 | Hapus UFW rule 5432 (tcp+udp) | 🔴 High | Agent |
| 3 | Konfirmasi host-Ollama masih dibutuhkan | 🟡 Medium | Fahmi |
| 4 | Bind MariaDB ke localhost jika tidak butuh remote | 🟡 Medium | Agent |
| 5 | Day 3: FastAPI scaffold + health endpoint | 🟢 Normal | Agent |
| 6 | Day 4: Auth endpoints + DB migration runner | 🟢 Normal | Agent |

---

*Generated automatically by agent audit script. Do not edit manually unless you know what you're doing.*
