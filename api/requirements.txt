# Web Framework
fastapi==0.115.0
uvicorn[standard]==0.32.0

# Auth
pyjwt[crypto]>=2.10.1,<3  # Day 26: bumped from 2.9.0 — mcp>=1.27 requires >=2.10.1
cryptography==43.0.0
pwdlib[argon2]==0.2.1

# Database
sqlalchemy==2.0.35
asyncpg==0.29.0
alembic==1.13.3
psycopg2-binary==2.9.9

# Vector & Memory
qdrant-client==1.12.0
fastembed==0.5.0

# LangGraph & LLM
langgraph==0.2.45
langchain==0.3.0
langchain-community==0.3.0
openai==1.52.0

# Celery & Cache
celery[redis]==5.4.0
redis==5.1.0

# Observability
structlog==24.4.0
langfuse==2.53.0

# Rate Limiting
slowapi==0.1.9

# Utilities
pydantic[email]>=2.11,<3  # Day 26: bumped from 2.9.0 — mcp>=1.27 requires >=2.11
pydantic-settings>=2.5.2,<3  # Day 26: bumped from 2.5.0 — mcp>=1.27 requires >=2.5.2
python-dotenv==1.0.0
httpx>=0.27.2,<0.28  # Day 26: bumped from 0.27.0 — mcp>=1.27 requires >=0.27.1
aiofiles==24.1.0
pyyaml>=6.0  # Day 31: personality templates (config/personalities.yaml)

# Day 26 — MCP Server (Streamable HTTP)
mcp>=1.27.0

# Day 38 — Magpie-Qwen2.5-Pro-300K-Filtered seed loader (HuggingFace parquet read)
pyarrow>=15.0

# Day 38 — multipart form for STT audio upload
python-multipart>=0.0.18

# Day 41 — export_pdf (markdown → styled PDF, pure-Python no Chromium)
weasyprint>=62
markdown>=3.6

# Testing
pytest==8.3.0
pytest-asyncio==0.24.0
