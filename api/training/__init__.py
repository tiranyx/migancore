"""MiganForge — Closed-loop training infrastructure.

Modules:
    data_exporter     — Extract preference_pairs from PostgreSQL
    dpo_trainer       — Standalone DPO training (runs on cloud GPU)
    runpod_client     — RunPod API launcher + monitor
"""
