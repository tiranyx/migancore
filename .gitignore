# Environment
.env
.env.local
.env.*.local

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
.venv/
ENV/
env/
api/.venv/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Data volumes (Docker)
data/
ollama-data/
pg-data/
qdrant-data/

# Secrets
/etc/ado/keys/
*.pem
*.key

# Logs
*.log
logs/

# Testing
.pytest_cache/
.coverage
htmlcov/

# ML artifacts (root only — api/models/ is SQLAlchemy ORM)
/models/
/checkpoints/
/lora_adapters/
/datasets/
*.gguf
*.safetensors

# Day 26: tarballs created during ad-hoc deploys/backups
api_day11.tar.gz
*.tar.gz

# Day 68 (Codex C2): artifacts generated by training cycles on server.
# These belong in /opt/ado-artifacts/ (sister dir), never in repo.
cycle*_output/
cycle*_failed_attempts/
docs_pending/
*.bak
*.bak_*
docker-compose.yml.bak

# Eval results: keep eval/baseline_*.json (anchors), ignore per-cycle results
# (results are reproducible from adapter + script, no need to version)
eval/eval_result_cycle*.json

# Day 68: nested migancore/ backup dir on server (api_backup leftover)
# This is a redundant copy, real source is api/. Should NEVER be in tree.
/migancore/

# Day 68: working scratch
TASK_BOARD.md


# Bundle files
*.bundle
migancore-push*.bundle

